'use client'
import AppHeader from '@/components/AppHeader'
import VoiceField from '@/components/VoiceField'
import {useState} from 'react'
import {useRouter} from 'next/navigation'
export default function Internal(){
  const r=useRouter();const[remaining,setRemaining]=useState('');const[parts,setParts]=useState('');const[sales,setSales]=useState('');const[note,setNote]=useState('');const[preview,setPreview]=useState(false);const[done,setDone]=useState(false)
  return <><AppHeader/><main className="form-shell"><section className="form-card wide"><div className="form-title"><div><p className="eyebrow">INTERNAL REPORT</p><h1>社内用報告書 作成</h1><p>客先提出済みの内容を引き継ぎ、社内向け情報を追記します。</p></div><span className="status ok">客先提出済</span></div><div className="internal-tabs" aria-label="社内用報告書の入力項目"><span className="active">1 基本情報</span><span>2 今回作業時の残作業</span><span>3 再手配の必要な部材</span><span>4 移動・作業時間</span><span>5 営業アプローチ</span><span>6 備考</span></div><div className="summary-strip"><div><span>病院名</span><b>東京中央医療センター</b></div><div><span>作業日</span><b>2026/09/05</b></div><div><span>作業者</span><b>山田 太郎</b></div></div><div className="internal-grid"><label>今回作業時の残作業<VoiceField multiline value={remaining} onChange={setRemaining} placeholder="残作業がある場合に入力"/></label><label>再手配の必要な部材<VoiceField multiline value={parts} onChange={setParts} placeholder="再手配が必要な部材を入力"/></label><label>客先への営業アプローチ<VoiceField multiline value={sales} onChange={setSales} placeholder="提案・要望などを入力"/></label><label>備考（社内への報告事項）<VoiceField multiline value={note} onChange={setNote} placeholder="社内共有事項を入力"/></label></div><div className="time-entry"><h3>移動および作業時間の推移</h3><div><label>移動 <input type="time" defaultValue="08:00"/> ～ <input type="time" defaultValue="09:00"/></label><label>作業 <input type="time" defaultValue="09:00"/> ～ <input type="time" defaultValue="15:30"/></label></div></div>{done&&<div className="inline-success">✓ 社内用報告書を完了しました。</div>}<div className="form-actions"><button className="secondary" onClick={()=>setPreview(true)}>プレビュー</button><button className="primary" onClick={()=>setDone(true)}>社内用報告書を完了</button></div></section></main>{preview&&<div className="demo-modal-backdrop" onClick={()=>setPreview(false)}><div className="demo-modal" onClick={e=>e.stopPropagation()}><div className="demo-modal-head"><div><p className="eyebrow">INTERNAL PDF PREVIEW</p><h2>社内用報告書</h2></div><button onClick={()=>setPreview(false)}>×</button></div><div className="report-paper internal-paper"><h3>社 内 用 報 告 書</h3><div className="paper-meta"><span>病院名</span><b>東京中央医療センター</b><span>作業日</span><b>2026/09/05</b><span>作業者</span><b>山田 太郎</b></div><div className="paper-section"><b>今回作業時の残作業</b><p>{remaining||'特になし'}</p></div><div className="paper-section"><b>再手配の必要な部材</b><p>{parts||'特になし'}</p></div><div className="paper-section"><b>客先への営業アプローチ・備考</b><p>{sales||'特になし'}{note&&`\n${note}`}</p></div></div><div className="demo-modal-actions"><button className="secondary" onClick={()=>setPreview(false)}>閉じる</button><button className="primary" onClick={()=>window.print()}>印刷</button></div></div></div>}</>}
